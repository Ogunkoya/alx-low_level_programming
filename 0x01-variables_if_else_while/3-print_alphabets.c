#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - Entry point
*
* Description: 'the program's description'
*
* Return: Always 0 (Success)
*/

int main(void)
{
int ch;
int ch1;

for (ch = 97; ch <= 122; ch++)
{
putchar(ch);
}
for (ch1 = 65; ch1 <= 90; ch++)
{
putchar(ch1)
}
putchar(10);

return (0);
}
