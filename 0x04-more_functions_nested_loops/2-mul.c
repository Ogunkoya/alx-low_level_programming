#include "main.h"

/**
* mul - Check if paramter c represents an uppercase letter.
* @a: Int to be multiple with a
* @b: Int to be multiple with b
* Return: Always 0.
*/

int mul(int a, int b)
{
	int result;

	result = a * b;
return (result);
}
